---
import type { OptionsDatabase } from '../types'
import type { ClubsPropsAdminPages } from '@devprotocol/clubs-core'
import Feed_ from '../components/Admin/Feed.vue'

type Props = ClubsPropsAdminPages & {
	feeds: OptionsDatabase[]
	edit?: OptionsDatabase
}
---

<Feed_ client:load {...Astro.props} />
